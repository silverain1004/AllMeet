# AllMeet — 주간보고초안 & 데이터 연결 기능

- 작성자: 서영은 (kkami4182@vntgcorp.com)
- 작성일: 2026-05-12
- 대상 서비스: `all-meet-agent` (Cloud Run / Google Chat 봇)

## 개요

AllMeet 봇에 **주간보고초안 자동 생성** 기능과, 이를 지탱하는 **사용자 데이터 연결(OAuth)** 흐름을 구축했습니다. 사용자가 챗에서 "주간보고초안" 한 마디만 보내면, 팀 캘린더·공유 드라이브·Confluence·본인 Gmail·개인 캘린더·내 드라이브의 한 주 활동을 자동으로 모아 카드로 정리하고, 그중 프로젝트 / 운영지원 두 카테고리로 분류한 종합 초안까지 LLM 으로 생성합니다. 사용자는 결과를 그대로 복사해 주간보고에 옮기면 됩니다.

---

## 1. 사용자 데이터 연결 (OAuth)

봇이 사용자의 Gmail / 개인 Calendar / 내 Drive 를 조회하려면 본인 동의가 한 번 필요합니다. 별도 메뉴 없이 두 가지 경로 중 어느 것이든 진입할 수 있도록 설계했습니다.

### 1.1 진입 경로

- **봇이 스페이스에 추가될 때**: 자동으로 환영 카드가 뜨면서 "🔗 내 데이터 연결" 버튼이 함께 표시됩니다. 사용자가 별도 안내 없이도 다음 단계를 알 수 있도록 설정이 가장 먼저 유도됩니다.
- **"설정" 키워드**: 챗에 `설정` 만 보내도 동일한 데이터 연결 카드가 즉시 뜹니다. (`데이터 연결`, `oauth 연결` 등 동의어도 매칭)

### 1.2 동의 흐름

1. 카드의 **🔗 Google 동의 페이지 열기** 버튼 클릭
2. 브라우저에서 Google 동의 화면 — Gmail · 개인 Calendar · 내 Drive 의 **읽기 전용** 권한
3. 동의 완료 → "연결 완료 ✅" 페이지 표시
4. **챗 스페이스로 봇이 자동으로 "제가 잘하는 업무는…" 안내 메시지를 푸시** — 사용자가 챗으로 돌아오면 자연스러운 다음 단계 안내

### 1.3 연결되는 데이터

| 영역 | 용도 | 권한 범위 |
| --- | --- | --- |
| Gmail | 한 주 메일을 주간보고초안에 반영 | 읽기 전용 |
| 개인 Calendar (primary) | 본인 일정·휴가·회의 반영 | 읽기 전용 |
| 내 Drive (My Drive) | 본인이 작성/수정한 파일 반영 | 읽기 전용 |

> 권한은 모두 readonly 입니다. 봇이 파일을 만들거나 수정하지 않습니다.

---

## 2. 주간보고초안 자동 생성

### 2.1 트리거

챗에 다음 키워드 중 하나를 입력:

- `주간보고초안` (권장)
- `주간 보고 초안`
- `주간보고 초안`

### 2.2 처리 흐름

1. **즉시 응답**: "주간보고초안 분석 중이에요. 잠시 후 결과를 보내드릴게요." (Google Chat 30초 응답 제한을 회피)
2. **백그라운드 thread** 가 데이터 수집 → 코드 단 사전 처리(필터·그룹화) → LLM 분석을 진행
3. 완료되면 Google Chat REST API 로 같은 스페이스에 결과 카드 푸시

### 2.3 수집 데이터 (6개 영역)

| 영역 | 데이터 출처 | 인증 방식 |
| --- | ---------- | ------- |
| 회의 이력 | 팀 공용 Google Calendar | 봇 서비스 계정 |
| 팀 공유 Drive | 팀이 등록한 Shared Drive(들) | 봇 서비스 계정 |
| Confluence 페이지 | 팀이 설정한 Space 의 페이지 | 글로벌 토큰 |
| Gmail | 본인 메일함 | 본인 OAuth |
| 개인 일정 | 본인 primary Calendar | 본인 OAuth |
| 내 드라이브 | 본인 My Drive 파일 | 본인 OAuth |

- 시간 범위: 회의 일자 기준 **회의주 + 전주** (2주 윈도우)
- 회의 일자는 팀 캘린더에서 "주간회의" 키워드로 자동 탐색, 없으면 KST 기준 폴백
- 모든 외부 API 호출은 try/except 로 격리 — 한 영역이 실패해도 나머지는 정상 표시

### 2.4 종합 초안 — LLM 분류 (Vertex Gemini)

수집·사전 정리된 데이터를 Vertex AI Gemini 에 넘겨 두 카테고리로 자동 분류:

- **프로젝트**: 신규 개발·설계·기능 구축·산출물 작성 등 주도적 업무
- **운영지원**: 시스템 유지보수, 정기 운영, SR/이슈 대응, 요청 처리, 협조 요청 등 지원성 업무

각 카테고리는 큰 단위 업무(task) 배열이고, 항목마다:
- `task` — 프로젝트/시스템 단위의 큰 업무명 (예: `[AllMeet] 주간보고 자동화`, `E-OUT 시스템 유지보수`)
- `details` — 그 업무를 구성한 1~8개의 세부 활동. distinct 한 이슈/산출물은 한 줄씩 분리, 같은 사안의 알림 시리즈는 한 줄로 압축

세부 작성 룰은 §2.8 참고.

### 2.5 출력 카드 구성

```
[헤더]    서영은님의 한 주 활동
          PC2팀 · 2026-05-14 (Thu)
          서영은 (kkami4182@vntgcorp.com)
          팀: PC2팀
          주간회의 일자: 2026-05-14 (Thu)

[리스트]  📅 회의 이력
          📁 Drive 작성/수정 파일
          📄 Confluence 페이지
          📧 Gmail
          🗓️ 개인 일정
          📁 내 드라이브

[종합]    ✏️ 종합 초안
          프로젝트
          • {task1}
            ◦ {detail1}
            ◦ {detail2}
          • {task2}
            ◦ {detail}
                              ← 카테고리 간에만 빈 줄 한 칸
          운영지원
          • {task}
            ◦ {detail}
```

- OAuth 미연결 영역은 `🔒 OAuth 미연결 — '내 데이터 연결' 카드를 클릭해 권한을 부여해 주세요` 로 안내
- 팀 공유 드라이브 미등록은 `⚠️ Shared Drive ID 미설정` 으로 안내
- 한 카테고리가 비어 있으면 그 카테고리는 통째 생략 (가짜 결과 강요 방지)
- 카테고리 안의 task 들은 붙어서 표시, 카테고리(프로젝트 ↔ 운영지원) 사이만 빈 줄 한 칸으로 구분

### 2.6 데이터 정합성 보장 룰 (코드 단)

| 룰 | 적용 범위 | 동작 |
| --- | --- | --- |
| 본인이 마지막 수정한 파일만 노출 | 카드+LLM | Drive API 응답의 `lastModifyingUser.emailAddress` 와 사용자 이메일 비교. 다른 팀원이 수정한 파일이 본인 주간보고에 섞이지 않음 |
| Gmail 노이즈 사전 제거 | 카드+LLM | 동료 발신 공유·후기·안내·공지·생산계획·초대장 메일, Claude.ai 로그인 보안 링크, Google 보안 알림, 뉴스레터(Claude Team, Mermaid 등), Confluence 일일 다이제스트, Slashpage 알림, 광고, no-reply 발신자 제거. 본인 발신 메일은 항상 통과 |
| 팀 캘린더 이벤트 중 본인 참여 건만 포함 | 카드+LLM | attendees 에 본인 이메일이 있고 declined 가 아닌 경우, 또는 organizer/creator 가 본인인 경우만 통과 |
| 주간회의 자체는 종합 초안에서 제외 | LLM 만 | 카드 리스트에는 표시되지만, 종합 초안의 "주간회의 진행" 같은 자기참조 요약은 방지. 회의 일자 컨텍스트는 그대로 확인 가능 |

### 2.7 이메일 사전 그룹화 (코드 단)

LLM 이 같은 사안의 알림 시리즈를 별도 task 로 쪼개거나, 같은 프로젝트의 distinct 이슈를 한 줄로 압축하는 사고를 막기 위해, 코드에서 결정론적으로 사전 그룹화한 뒤 LLM 에 전달합니다.

**그룹화 키 우선순위** (`prompts._group_key`):
1. **SR 번호** (예: `SR2605-00483`) — 같은 SR 의 모든 알림(접수·결재·종료)이 한 그룹
2. **Jira 키 prefix** (예: `AMT-9`, `AMT-11` → AMT) — 같은 프로젝트의 모든 이슈가 한 그룹
3. **정규화 제목** — 알림 prefix (`[ITS …]`, `[결재 …]`, `Re:`, `Fw:` 등)·트레일링 상태 표기(`(승인완료)` 등) 제거 후 같은 본질 제목이면 한 그룹

**프로젝트 매핑** (`prompts._KNOWN_PROJECTS`):
```python
_KNOWN_PROJECTS = {
    "AMT": "[AllMeet] 주간보고 자동화",
}
```
새 prefix(MES-, ERP- 등) 등장 시 한 줄 추가. 매핑 없는 prefix 는 자동으로 `"{prefix} Jira 프로젝트"` 로 폴백.

**LLM 에 전달되는 그룹 헤더 예시**:
```
### [AllMeet] 주간보고 자동화  (Jira AMT-* 6건)  — 이 그룹은 반드시 하나의 task 로 묶고, 쪼개지 마세요
- [Jira] (AMT-9) Confluence 자동화 ...
- [Jira] (AMT-11) Vertex AI Search ...
- ...

### 스마트베스틸 경조사 시스템 신규 구축 요청  (같은 사안 알림 3건)  — ...
- [ITS 변경종료 알림] 스마트베스틸 경조사 시스템 신규 구축 요청
- [결재 완료 알림] [ITS 요청서] 스마트베스틸 경조사 시스템 신규 구축 요청 (승인완료)
- [ITS 서비스요청 접수완료알림] 스마트베스틸 경조사 시스템 신규 구축 요청
```

타입 힌트(`Jira AMT-* 6건` / `SR 관련 메일 X건` / `같은 사안 알림 X건`)로 LLM 이 압축 vs 분리를 판단:
- **Jira 그룹**: 각 이슈가 distinct → details 한 줄씩 분리
- **SR/같은 사안 그룹**: 같은 사안의 알림 시리즈 → 한 줄로 압축 OK

### 2.8 종합 초안 작성 룰 (LLM 프롬프트)

| 룰 | 동작 |
| --- | --- |
| 본인 발신 메일은 반드시 포함 | `from` 이 사용자 이메일과 일치 = 본인이 한 행위. 단일이라도 누락 금지 |
| 수신 정보 메일은 업무 아님 | 알림 시리즈(ITS 접수·결재·종료 등) 만 본인 트리거 워크플로우로 인정. 그 외 단순 수신 정보는 task 화 금지. ※ 명백한 노이즈(공유/후기/안내/공지/생산계획/초대장/뉴스레터/광고/보안 알림 등)는 코드 단에서 이미 제거됨 |
| 알림 메일은 행위로 변환 | `[ITS 서비스요청 접수완료알림] X` → `X 요청 접수`, `[ITS 변경종료 알림] X` → `X 신규 구축 완료`, `[결재 완료 알림] X (승인완료)` → `X 결재 완료` |
| 압축 vs 분리 구분 | 같은 SR/이슈의 알림 시리즈(접수 → 결재 → 종료)는 한 줄로 압축. 같은 프로젝트의 distinct 이슈/산출물은 한 줄씩 분리 |
| 행위 동사로 마무리 | 산출물 → `… 작성`, 요청 → `… 요청`, 자료 공유 → `… 공유`. `~드립니다` 같은 정중표현 제거 |
| 노이즈 부기 금지 | 본인 이메일/이름, Jira 메일의 `(최은비 assign)` 같은 시스템 부착 텍스트, `귀하에게 할당했습니다` 같은 Jira 알림 문구 부기 금지 |
| 담당자 부기는 다른 사람만 | 본인이 아닌 담당자가 데이터에 명시돼 있을 때만 `(이름 직급)` 부기 |
| 추측 금지 | 데이터에 없는 진척률·기한·담당자 생성 금지 |
| 회사 시스템 약어 보존 | E-OUT, E-BIZ, MES, ITS, SAP, SR, ERP, GWS 등 그대로 유지 |

### 2.9 팀 주간보고 표준 포맷 (참고)

PC2팀에서 실제 사용 중인 주간보고 양식은 아래와 같은 3단 구조입니다 — LLM 출력은 이 구조에 최대한 가깝게 맞춥니다.

```
프로젝트
[Hackathon] Agentic Workflow              ← 프로젝트 단위 큰 묶음 (대괄호 prefix 권장)
25% (6/19)                                  ← 진척률·sprint 정보 (수동 추가)
sprint #3 (5/1-5/15)
  코드리뷰 및 리팩터링(project, firestore 등)   ← 세부 작업
  Google Calendar 관련 기능 구현
  RAG 구축 및 연동
  주간보고 관련 기능 구현

운영지원
E-OUT 시스템 유지보수                       ← 시스템 단위 큰 묶음
100%
  [SR2605-00483] 특수강 실적등록 시 에러 발생(대형압연파트 양승민대리)
    EOSP100E02 실적등록 화면에서 저장 시 오류 팝업 발생
    MES 원데이터 불일치로 인해 SPPH4010 업데이트 대상이 조회되지 않아 updateCnt=0 발생
    생산관리 담당자에게 이슈 전달 및 원데이터 정합성 점검(송원용매니저)
    이슈 조치 완료
```

LLM 출력에서 자동 채워주는 부분:
- 프로젝트/시스템 umbrella 명 (대괄호 prefix)
- 세부 작업 묶음 (각 distinct 이슈 한 줄씩)
- SR 번호·Jira 키 등 데이터에 있는 식별자 보존
- 본인이 아닌 담당자가 데이터에 있으면 (괄호) 로 부기
- 행위 동사로 마무리(작성·요청·공유·완료 등)

수동 작성이 필요한 부분 (LLM 이 추측하지 않음):
- 진척률 (%, 6/19 등)
- Sprint 정보
- 기한(`~5월 말`)
- 데이터에 없는 담당자·이슈 배경

### 2.10 출력 예시 (실제 LLM 결과)

```
프로젝트
• [AllMeet] 주간보고 자동화
  ◦ AMT-9: Confluence 자동화 로직 개발
  ◦ AMT-11: Vertex AI Search 셋업 및 검색 기능 구현
  ◦ AMT-34: 메모리시스템 (Firestore) 연동
  ◦ AMT-36: RAG 구축 (Vertex AI Search)
  ◦ AMT-37: RAG 연동 (Drive, Calendar, Confluence)
  ◦ AMT-41: 주간보고 시스템 구축
• Agentic Workflow 기반 업무 수행 방식 혁신 제안서 ver2.0 작성
• ISA95_AI_Architecture.pptx 작성
• WIS 2026 글로벌 전망 콘퍼런스 참석 후기 및 인사이트 공유

운영지원
• MES 신규 개발 SR 요청
  ◦ MES 신규 개발 건 SR 요청
• [세아웍스] 경조사 게시판 데이터 요청
  ◦ 접수·결재 완료
• 스마트베스틸 경조사 시스템 신규 구축
  ◦ 접수·결재·변경 종료 완료
• (MES) 에버가드 IDC 운영 일지 신규 화면 개발
  ◦ 요청 접수 및 변경관리 이관, 결재 완료
```

위 예시에서 자동으로 적용된 코드/프롬프트 룰:
- AMT-* 가 코드 단 사전 그룹화로 하나의 task 안에 묶임
- 각 AMT 이슈는 distinct 라 한 줄씩 분리, `(최은비 assign)` 같은 시스템 메타 제거
- ITS 알림 시리즈(접수·결재·종료)는 같은 사안이라 한 줄로 압축
- 본인 발신 메일(`WIS 2026 ... 공유`, `MES 신규 개발 SR 요청드립니다`)은 정중표현 제거 후 행위 동사로
- 동료 발신 정보 공유·안내·후기·공지·생산계획·초대장 메일(AWS LLM 워크숍 후기, ITS 프로세스 변경 안내, 사내 행사 초대장 등)은 코드 단에서 사전 제거
- 본인 이메일·본인 이름은 details 에 부기하지 않음
- task 들은 카테고리 안에서 붙어 표시, 프로젝트 ↔ 운영지원 사이만 빈 줄

---

## 3. 팀 설정 메뉴 (기반 인프라)

주간보고초안이 동작하려면 팀 단위 설정이 선행되어야 합니다. 챗 카드 기반 UI 로 모두 챗에서 직접 관리할 수 있습니다.

### 3.1 메뉴 구성

```
주간회의/주간보고
├ 1. 일정 조회
│   ├ 주간회의 일정 (이번주 / 다음주)
│   ├ 팀원 휴가일정 (이번주 / 다음주)
│   └ 캘린더 ID 설정
├ 2. 팀 설정
│   ├ 조회 / 추가 / 수정 / 삭제
│   └ Shared Drive ID 설정      ← 팀별 공유 드라이브 등록
├ 3. 팀원 설정
│   └ 조회 / 추가 / 수정 / 삭제 / 순서 변경
├ 4. 컨플루언스 설정
│   ├ 스페이스 키 / 루트 페이지 / 템플릿 페이지
│   └ 조회 / 수정
└ 5. 스케줄러
    └ 주간회의 일자 자동 탐색 테스트
```

### 3.2 Shared Drive 자동 권한 부여

팀 Shared Drive ID 를 저장할 때:
1. 봇 서비스 계정이 이미 멤버이면 즉시 통과
2. 멤버가 아니면 **요청자의 OAuth 권한으로 봇 SA 를 reader 로 자동 등록 시도**
3. 요청자가 Manager 가 아니면 안내 메시지로 수동 추가 방법 안내

### 3.3 사용자 ↔ 팀 매핑

- 사용자 이메일로 팀을 자동 매칭 (`firestore.team_config.find_team_by_email`)
- 한 사용자가 여러 팀에 속할 수 있으나 주간보고초안은 첫 매칭 팀 기준

---

## 4. 기술 아키텍처

| 영역 | 사용 기술 |
| --- | --- |
| 런타임 | Cloud Run (asia-northeast3), Python 3.12, Functions Framework |
| LLM | Vertex AI Gemini (`gemini-2.0-flash-001`) — 환경변수로 모델 교체 가능 |
| 응답 형식 | JSON structured output (`response_mime_type=application/json` + `response_schema`) |
| 데이터 저장 | Firestore (팀 설정, OAuth refresh_token, 대화 컨텍스트) |
| 인증 | (1) 서비스 계정 자동 인증 (팀 데이터) (2) OAuth 2.0 Authorization Code Flow (사용자 데이터) |
| 비동기 | 즉시 응답 + Python `threading.Thread` 백그라운드 + Chat REST API push |
| 분기 라우팅 | 키워드/정규식 기반 의도 분류 (`UserIntent` enum), Python 3.10 structural pattern matching |

---

## 5. 사용자 입장의 프로세스 (End-to-End Walkthrough)

크게 세 단계 — **① 셋업 (최초 1회) → ② 사용 (매주 반복) → ③ 결과 활용**.

### 5.1 봇 첫 추가 (관리자 1회)

팀 리더가 Google Chat 스페이스에 AllMeet 봇을 추가합니다.

→ 봇이 자동으로 환영 카드를 띄움:
```
All-Meet 입니다. 먼저 '내 데이터 연결' 을 한 번 설정해 주세요.

┌────────────────────────────────────┐
│ All-Meet 가 추가됐어요 👋          │
│ Gmail · 개인 Calendar · 내 Drive   │
│ 활동을 주간보고초안에 함께 반영하   │
│ 려면 '내 데이터 연결' 한 번이 필요  │
│ 해요. (한 번만 동의하면 됩니다)    │
│                                    │
│ [🔗 내 데이터 연결]                 │
└────────────────────────────────────┘
```

### 5.2 데이터 연결 (각 팀원 1회)

각 팀원이 봇의 환영 카드 또는 챗에 `설정` 입력 → 데이터 연결 카드:

```
┌──────────────────────────────────────┐
│ 설정 — 내 데이터 연결                 │
│ Gmail · 개인 Calendar · 내 Drive 를  │
│ 봇과 연결합니다.                     │
│                                      │
│ [🔗 내 데이터 연결 (Gmail/Cal/Drive)] │
└──────────────────────────────────────┘
```

→ 버튼 클릭 → Google 동의 화면 (브라우저 새 탭):
- 권한: Gmail 읽기, Calendar 읽기, Drive 읽기 (모두 readonly)
- 동의 → "연결 완료 ✅" 페이지

→ 챗으로 돌아오면 봇이 자동으로 메시지 전송:
```
All-Meet 입니다.
메시지를 보내 주시면 맞춰 도와드릴게요.
제가 잘하는 업무는 이런 게 있어요.
• 💬 일상 대화 · 질문 답변
• 🌐 최신 정보 (주가, 뉴스, 날씨 등)
• 🗓️ 캘린더·일정 관리
• 👥 주간 회의·팀/인원 등록
...
```

### 5.3 팀 셋업 (팀 매니저 1회)

팀 매니저가 챗에서 `팀 등록` 또는 `주간회의` 입력 → 메뉴 카드. 5개 메뉴를 순서대로 셋업:

1. **`2. 팀 설정`** → 추가 → 팀 ID, 팀 이름 입력
2. **`3. 팀원 설정`** → 추가 → 멤버 이름·닉네임·이메일 등록
3. **`1. 일정 조회` → 캘린더 ID 설정** → 팀 공용 Google Calendar ID 저장
4. **`2. 팀 설정` → Shared Drive ID 설정** → 팀 공유 Drive ID 저장 (봇 SA 자동 등록 시도)
5. **`4. 컨플루언스 설정`** → Space Key, 루트 페이지, 템플릿 페이지 등록

각 메뉴는 단계별 카드 입력 양식으로 진행되며, 저장 완료 시 ✅ 안내 메시지로 응답합니다.

### 5.4 주간보고초안 트리거 (각 팀원, 매주)

팀원이 챗에 **`주간보고초안`** 한 단어 입력:

→ 즉시 응답:
```
주간보고초안 분석 중이에요. 잠시 후 결과를 보내드릴게요.
```

→ 백그라운드 (보통 10~30초 후) 같은 스페이스에 결과 카드 푸시:
```
┌─────────────────────────────────────┐
│ 서영은님의 한 주 활동                 │
│ PC2팀 · 2026-05-14 (Thu)             │
│ 서영은 (kkami4182@vntgcorp.com)       │
│ 팀: PC2팀                            │
│ 주간회의 일자: 2026-05-14 (Thu)       │
│                                      │
│ 📅 회의 이력                          │
│ 1. PC2팀 주간회의 (2026-05-07)        │
│ 2. PC2팀 주간회의 (2026-05-14)        │
│                                      │
│ 📁 Drive 작성/수정 파일                │
│ 1. (파일이 있으면 여기에 나열)         │
│                                      │
│ 📄 Confluence 페이지                  │
│ 1. ...                               │
│                                      │
│ 📧 Gmail (노이즈 제거 후)             │
│ 1. ... (본인 발신·업무 메일만)         │
│                                      │
│ 🗓️ 개인 일정                          │
│ ...                                  │
│                                      │
│ 📁 내 드라이브                        │
│ ...                                  │
│                                      │
│ ✏️ 종합 초안                           │
│ 프로젝트                              │
│ • [AllMeet] 주간보고 자동화           │
│   ◦ AMT-9: Confluence 자동화 ...     │
│   ◦ AMT-11: Vertex AI Search ...     │
│ • Agentic Workflow ... 제안서 작성    │
│                                      │
│ 운영지원                              │
│ • MES 신규 개발 SR 요청               │
│   ◦ MES 신규 개발 건 SR 요청          │
│ • 스마트베스틸 경조사 시스템 신규 구축 │
│   ◦ 접수·결재·변경 종료 완료          │
└─────────────────────────────────────┘
```

### 5.5 결과 활용 (각 팀원, 매주)

✏️ 종합 초안 영역을 그대로 복사해서 본인 주간보고 양식에 붙여넣기.

자동 채워진 부분(LLM 처리):
- 프로젝트/시스템 단위 그룹핑
- 각 distinct 이슈/산출물 한 줄씩 정리
- 알림 시리즈 압축
- 행위 동사로 마무리

수동 보강이 필요한 부분 (LLM 이 추측하지 않음):
- 진척률 (예: `25% (6/19)`)
- Sprint 정보 (`sprint #3 (5/1-5/15)`)
- 기한 (`~5월 말`)
- 데이터에 없는 담당자·이슈 배경

### 5.6 권한 회수·재동의

사용자가 Google 계정에서 봇 권한 철회 → 다음 호출 때 `revoked` 로 자동 감지 → 카드에 `🔒 OAuth 미연결` 안내 → `설정` 입력해서 재동의로 복구.

### 사용자 입장 흐름 요약

```
[관리자]                          [팀 매니저]                       [각 팀원]
  │                                  │                                │
  │ 1. 봇 추가                       │                                │
  │    → 환영 카드                    │                                │
  │                                  │                                │
  │                                  │ 2. 팀·팀원·캘린더·드라이브·     │
  │                                  │    컨플루언스 셋업 (1회)         │
  │                                  │                                │
  │                                  │                                │ 3. "설정" → OAuth 동의 (1회)
  │                                  │                                │    → 자동 안내 메시지
  │                                  │                                │
  │                                  │                                │ 4. "주간보고초안" 입력 (매주)
  │                                  │                                │    → 한 주 활동 카드 + 종합 초안
  │                                  │                                │
  │                                  │                                │ 5. 복사 → 주간보고에 붙여넣기
  │                                  │                                │    + 진척률·기한 수동 보강
```

---

## 6. 보안·개인정보 처리

- **OAuth 토큰**: 사용자별 refresh_token 만 Firestore `oauth_tokens/{email}` 에 저장. access_token 은 메모리 캐시만 (TTL 자동 만료)
- **스코프**: 모두 readonly (Gmail readonly, Calendar readonly, Drive readonly) — 봇이 사용자 데이터를 쓰거나 변경하지 않음
- **데이터 격리**: 각 사용자의 주간보고초안은 본인 이메일·본인 OAuth 토큰 기준으로만 조회. A 의 보고에 B 의 데이터가 섞이지 않음
- **동의 철회**: 사용자가 Google 계정에서 봇 권한 철회 시 다음 호출에서 `revoked` 상태로 자동 감지

---

## 7. 다음 단계 후보 (백로그)

- 사용자가 카드에서 직접 종합 초안을 다듬는 "초안 수정" 액션 — 항목 합치기·빼기·재분류
- 한 사용자가 여러 팀에 속한 경우의 라우팅 정책 (현재 첫 매칭 팀)
- 새 Jira 프로젝트(MES-, ERP- 등) 자동 학습 — 현재는 `_KNOWN_PROJECTS` 에 수동 등록 필요
- OAuth state 의 nonce + server-side session 도입 (운영 보안 강화)
- 주간보고 페이지(Confluence) 로 결과를 1-클릭 업로드하는 액션
- Confluence 페이지 후처리에 `lastModifyingUser` 정합성 필터 적용 검토
- 종합 초안의 진척률·sprint·기한 같은 메타데이터를 별도 입력 UI 로 보강
