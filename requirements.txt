# Cloud Run / Functions Framework: main.py hello_http entry (see Procfile web).
functions-framework>=3.0,<4

# Vertex AI Gemini for domains/daily_chat. Keep sync responses fast (Chat ~30s limit).
google-cloud-aiplatform>=1.38.0

# Python version: must match runtime.txt (python-3.12).
